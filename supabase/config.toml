project_id = "replace-with-your-supabase-project-ref"

[functions.onboarding-webhook]
verify_jwt = true

# Database Webhook historique (désactivé — plus de relay n8n évaluation)
[functions.item-intake-evaluation-webhook]
verify_jwt = false

/**
 * Kill switch UI « prêts » (dépôt de pièces membres → Segna).
 * Le produit est passé en location / achat uniquement ; le pipeline intake
 * reste en base pour l’historique, mais n’est plus exposé dans l’app.
 */
export const MEMBER_LENDING_UI_ENABLED = false;

/**
 * Provisoire : KYC non obligatoire pour l’emprunt (paiement panier) ni dans l’onboarding in-app.
 * Repasser à `true` pour réactiver l’exigence KYC.
 */
export const KYC_REQUIRED_FOR_BORROW = false;

/** Étape KYC dans le parcours onboarding in-app (intro → profile → …). */
export const KYC_INCLUDED_IN_ONBOARDING = false;

# Onboarding feature module

- This module will own step definitions, validation schema, and persistence contract.
- Keep UI components small and composable by step.
- Persist data through CRUD and RPC only.
